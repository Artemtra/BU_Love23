<Window x:Class="BU_Love.Views.ProfileWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Профиль" Width="800" Height="800"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent">

    <Border Background="#1a1a1a" CornerRadius="15" BorderBrush="#FF9800" BorderThickness="2">
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Заголовок -->
            <Grid Grid.Row="0" Margin="0,0,0,20">
                <TextBlock x:Name="ProfileTitle" Text="👤 Профиль" FontSize="28" FontWeight="Bold" 
                           Foreground="#FF9800" HorizontalAlignment="Left"/>
                <Button Content="✕" Click="CloseButton_Click" HorizontalAlignment="Right"
                        Background="#ff4444" Foreground="White" Width="40" Height="40"
                        FontSize="20" BorderThickness="0" Cursor="Hand"/>
            </Grid>

            <!-- Контент -->
            <StackPanel Grid.Row="1">
                <!-- Информация о пользователе -->
                <Border Background="#2d2d2d" CornerRadius="10" Padding="20" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock x:Name="UsernameText" FontSize="24" FontWeight="Bold" Foreground="White"/>
                        <TextBlock x:Name="PhoneText" FontSize="16" Foreground="#aaaaaa" Margin="0,5,0,0"/>
                        <TextBlock x:Name="AddressText" FontSize="16" Foreground="#aaaaaa" Margin="0,5,0,0"/>
                        <Border Background="#4CAF50" CornerRadius="5" Padding="10,5" 
                                HorizontalAlignment="Left" Margin="0,10,0,0">
                            <TextBlock x:Name="BonusText" FontSize="18" FontWeight="Bold" Foreground="White"/>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- Кнопки действий -->
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0" Content="🛒 КОРЗИНА" Click="CartButton_Click"
                            Background="#2196F3" Foreground="White" FontSize="18" FontWeight="Bold"
                            Padding="20,15" BorderThickness="0" Cursor="Hand" Margin="0,0,10,0"/>

                    <Button Grid.Column="1" Content="📋 ИСТОРИЯ ЗАКАЗОВ" Click="OrdersButton_Click"
                            Background="#FF9800" Foreground="White" FontSize="18" FontWeight="Bold"
                            Padding="20,15" BorderThickness="0" Cursor="Hand"/>
                </Grid>

                <!-- История заказов -->
                <ScrollViewer x:Name="OrdersScrollViewer" Visibility="Collapsed" MaxHeight="550">
                    <StackPanel x:Name="OrdersPanel"/>
                </ScrollViewer>
            </StackPanel>

            <!-- Кнопка выхода -->
            <Button Grid.Row="2" Content="🚪 ВЫЙТИ ИЗ АККАУНТА" Click="LogoutButton_Click"
                    Background="#ff4444" Foreground="White" FontSize="16" FontWeight="Bold"
                    Padding="15" BorderThickness="0" Cursor="Hand"/>
        </Grid>
    </Border>
</Window>